# ローカル開発用。`cp .env.local.example .env.local` でそのまま使えます。
#
# ここに書かれているのは Supabase CLI がローカルで必ず生成する固定値です。
# 本番のキーではないので、この状態でコミットして問題ありません。
# （値が違う場合は `npm run db:status` で確認してください）

NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
